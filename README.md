Python 3.11+
├── openai          → Whisper (transcripción) + GPT si querés
├── anthropic       → Claude para el análisis
├── typer           → CLI linda y simple
├── ffmpeg-python   → Extraer audio del video
└── JSON files      → Persistencia local (sin DB por ahora)
